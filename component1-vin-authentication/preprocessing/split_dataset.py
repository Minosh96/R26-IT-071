import os
import shutil
import random

def split_data(source_dir, train_dir, test_dir, split_ratio=0.8):
    """
    Split the dataset into training and testing sets.
    """
    print(f"Splitting data from {source_dir} into {train_dir} and {test_dir}...")
    # Logic to move/copy files

if __name__ == "__main__":
    print("Split dataset script initialized.")
