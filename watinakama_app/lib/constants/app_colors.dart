import 'package:flutter/material.dart';

class AppColors {
  // Backgrounds
  static const Color lightBlueTop = Color(0xFFB8D4F0);   // top section light blue
  static const Color darkNavyBg = Color(0xFF0D1117);      // main dark background
  static const Color darkNavyCard = Color(0xFF161B27);    // text field background
  static const Color darkNavySurface = Color(0xFF1A2235); // slightly lighter surface

  // Brand blue
  static const Color primaryBlue = Color(0xFF2563EB);     // main button blue
  static const Color linkBlue = Color(0xFF3B82F6);        // link text blue

  // Text
  static const Color textWhite = Color(0xFFFFFFFF);
  static const Color textGray = Color(0xFF8892A4);
  static const Color textDark = Color(0xFF1A1A2E);        // dark text on light bg
  static const Color textFieldBorder = Color(0xFF2A3347);

  // Status (for MHS score display later)
  static const Color statusGreen = Color(0xFF00E676);
  static const Color statusAmber = Color(0xFFFFB300);
  static const Color statusRed = Color(0xFFFF1744);
}
